{
  "name": "frontend",
  "version": "1.0.0",
  "description": "frontend for the coffeeshop app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@gitlab.orangeandbronze.com:2289/onb-way-project/coffeeshop/frontend.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  }
}
